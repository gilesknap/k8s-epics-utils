An image for developers to use for working on EPICS in containers

build your own instance of this using the following command, this will copy some
files like .gitconfig into the image to make a personal development environment

build-dev.sh


